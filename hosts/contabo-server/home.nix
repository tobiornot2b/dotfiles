{ config, pkgs, ... }:

{
  # Minimal home manager configuration for server
  # Root user has minimal customization
  home.stateVersion = "24.11";
  
  # No shell customization needed; kept minimal
}
